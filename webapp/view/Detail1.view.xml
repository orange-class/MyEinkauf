<mvc:View 
	xmlns:core="sap.ui.core" 
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns="sap.m" 
	xmlns:t="sap.ui.table" 
	xmlns:layout="sap.ui.layout" 
	xmlns:html="http://www.w3.org/1999/xhtml" 
	controllerName="qstMyEinkauf.controller.Detail1">
	<Page title="{orderName}" navButtonPress="onNavBack" showNavButton="{device>/system/phone}"> <!-- titel an Modell anpassen -->
		<content>
			
			<!-- UNTERTITEL: Eigentümer der Liste -->
			<!-- rechts daneben: zuletzt bearbeitet von wem und wann? -->
			
			<!-- Settings-Button -->
			<Button id="buttonToSettings" label="Einstellungen" icon="sap-icon://action-settings" press="onToSettings"/>
			
			<!-- SORTIEREN: alphabetisch, Ersteller, Zeit via Dropdown (ComboBox) -->
			<!-- Abgeschlossenes Ausblenden - Button -->
			<!-- Neuen Eintrag hinzufügen - Button -->
			
			<List id="products" items="{products}"> <!-- items an Modell anpassen -->
				<items>
					<StandardListItem title="{productName}" type="Active"/> <!-- press="onSelectionChange"-->
				</items>
			</List>
			<!--<Table
				id="table"
				selectionMode="MultiToggle"
				rows="{/products}"
				visibleRowCount="7"
				sort="sortDeliveryDate"
				ariaLabelledBy="title">
				<extension>
					<m:OverflowToolbar style="Clear">
						<m:Title id="title" text="Products"/>
						<m:ToolbarSpacer/>
						<m:Button
							icon="sap-icon://sorting-ranking"
							tooltip="Sort ascending across Categories and Name"
							press="sortCategoriesAndName"/>
						<m:Button
							icon="sap-icon://sort"
							tooltip="Sort Categories in addition to current sorting"
							press="sortCategories"/>
						<m:Button
							icon="sap-icon://decline"
							tooltip="Clear all sortings"
							press="clearAllSortings"/>
					</m:OverflowToolbar>
				</extension>
				<columns>
					<Column
						id="name"
						width="11rem"
						sortProperty="Name">
						<m:Label text="Product Name" />
						<template>
							<m:Text text="{Name}" wrapping="false" />
						</template>
					</Column>
					<Column
						id="categories"
						width="11rem"
						showSortMenuEntry="false"
						sortProperty="Category">
						<m:Label text="Category" />
						<template>
							<m:Text text="{Category}" wrapping="false" />
						</template>
					</Column>
					<Column width="9rem">
						<m:Label text="Image" />
						<template>
							<m:Link text="Show Image" href="{ProductPicUrl}" target="_blank"/>
						</template>
					</Column>
					<Column
						id="quantity"
						width="6rem"
						hAlign="End"
						sortProperty="Quantity">
						<m:Label text="Quantity" />
						<template>
							<m:Label text="{
								path: 'Quantity',
								type: 'sap.ui.model.type.Integer'
							}" />
						</template>
					</Column>
					<Column
						id="deliverydate"
						width="9rem"
						sortProperty="DeliveryDateStr">
						<m:Label text="Delivery Date" />
						<template>
							<m:Text text="{
								path: 'DeliveryDateStr',
								type: 'sap.ui.model.type.Date',
								formatOptions: {source: {pattern: 'dd/MM/yyyy'}, style: 'long'}
							}" wrapping="false" />
						</template>
					</Column>
				</columns>
				<footer>
					<m:OverflowToolbar id="infobar"></m:OverflowToolbar>
				</footer>
			</Table>-->
		</content>
	</Page>
</mvc:View>